{
  "name": "ts-csv-json5",
  "version": "0.0.1",
  "description": "A CSV and JSON5 tools for generate related TypeScript code.",
  "keywords": ["typescript", "CSV", "JSON5"],
  "author": "Sergio Lindau",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "update": "ncu -u",
    "prebuild": "rimraf dist",
    "build": "tsc",
    "build:dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ts-morph": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.7",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4"
  }
}
